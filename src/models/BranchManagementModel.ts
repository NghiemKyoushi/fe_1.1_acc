export interface colBranch {
  id: string;
  name: string;
  code: string;
  phoneNumber: string;
  accountNumber: string;
  bank: string;
}

export interface BranchParamSend {
  name: string;
  code: string;
  phoneNumber: string;
  accountNumber: string;
  bank: string;
}
